# Dependencies
node_modules

# Build output (Netlify rebuilds on deploy)
dist
.astro

# Editor / tooling working directories
.claude
.vscode
.idea
.DS_Store

# Tailored résumé PDFs at repo root (only public/ versions get published)
/*.pdf

# Local env
.env
.env.local
.env.*.local
